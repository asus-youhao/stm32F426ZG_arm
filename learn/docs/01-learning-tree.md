# 01 · 學習樹總覽頁（index.html）

- SVG 學習樹：L1→L2→L3 垂直階梯，右側分岔到專案 A/B（同 esp32 learn 的版型語彙）。
- 「教材 = 這個 repo 本身」對照表：每個學習主題對到 repo 實際檔案與層級。
- 職涯頁 callout。

## 設計決策

- 兩條專案路線直接取 repo 史實：A = `firmware/`（CANopen 主線）、
  B = `feature/linux-*-ethercat-master` 系列（EtherCAT 實戰，含最後被控制權鎖死改走
  CANopen 的結論）。失敗路線也入教材（見 06）。
